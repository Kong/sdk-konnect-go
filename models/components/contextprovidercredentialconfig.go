// Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.

package components

import (
	"encoding/json"
	"errors"
	"fmt"
	"github.com/Kong/sdk-konnect-go/internal/utils"
)

type ContextProviderCredentialConfigType string

const (
	ContextProviderCredentialConfigTypeBearer ContextProviderCredentialConfigType = "bearer"
)

// ContextProviderCredentialConfig - The provider-specific authentication configuration for the credential, as returned by the API. Never includes secret values — `secrets` is write-only and omitted entirely from every response.
type ContextProviderCredentialConfig struct {
	BearerContextProviderCredentialConfig *BearerContextProviderCredentialConfig `queryParam:"inline" union:"member"`

	Type ContextProviderCredentialConfigType
}

func CreateContextProviderCredentialConfigBearer(bearer BearerContextProviderCredentialConfig) ContextProviderCredentialConfig {
	typ := ContextProviderCredentialConfigTypeBearer

	typStr := BearerContextProviderCredentialConfigAuthType(typ)
	bearer.AuthType = typStr

	return ContextProviderCredentialConfig{
		BearerContextProviderCredentialConfig: &bearer,
		Type:                                  typ,
	}
}

func (u *ContextProviderCredentialConfig) UnmarshalJSON(data []byte) (err error) {
	previous := *u
	*u = ContextProviderCredentialConfig{}
	defer func() {
		if err != nil {
			*u = previous
		}
	}()

	type discriminator struct {
		AuthType string `json:"auth_type"`
	}

	dis := new(discriminator)
	if err := json.Unmarshal(data, &dis); err != nil {
		return fmt.Errorf("could not unmarshal discriminator: %w", err)
	}

	switch dis.AuthType {
	case "bearer":
		bearerContextProviderCredentialConfig := new(BearerContextProviderCredentialConfig)
		if err := utils.UnmarshalJSON(data, &bearerContextProviderCredentialConfig, "", true, nil); err != nil {
			return fmt.Errorf("could not unmarshal `%s` into expected (AuthType == bearer) type BearerContextProviderCredentialConfig within ContextProviderCredentialConfig: %w", string(data), err)
		}

		u.BearerContextProviderCredentialConfig = bearerContextProviderCredentialConfig
		u.Type = ContextProviderCredentialConfigTypeBearer
		return nil
	}

	return fmt.Errorf("could not unmarshal `%s` into any supported union types for ContextProviderCredentialConfig", string(data))
}

func (u ContextProviderCredentialConfig) MarshalJSON() ([]byte, error) {
	if u.BearerContextProviderCredentialConfig != nil {
		return utils.MarshalJSON(u.BearerContextProviderCredentialConfig, "", true)
	}

	return nil, errors.New("could not marshal union type ContextProviderCredentialConfig: all fields are null")
}
