# PartialModelHuggingface


## Fields

| Field                                    | Type                                     | Required                                 | Description                              |
| ---------------------------------------- | ---------------------------------------- | ---------------------------------------- | ---------------------------------------- |
| `UseCache`                               | `*bool`                                  | :heavy_minus_sign:                       | Use the cache layer on the inference API |
| `WaitForModel`                           | `*bool`                                  | :heavy_minus_sign:                       | Wait for the model if it is not ready    |