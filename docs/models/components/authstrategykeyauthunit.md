# AuthStrategyKeyAuthUnit

## Example Usage

```go
import (
	"github.com/Kong/sdk-konnect-go/models/components"
)

value := components.AuthStrategyKeyAuthUnitDays

// Open enum: custom values can be created with a direct type cast
custom := components.AuthStrategyKeyAuthUnit("custom_value")
```


## Values

| Name                           | Value                          |
| ------------------------------ | ------------------------------ |
| `AuthStrategyKeyAuthUnitDays`  | days                           |
| `AuthStrategyKeyAuthUnitWeeks` | weeks                          |
| `AuthStrategyKeyAuthUnitYears` | years                          |