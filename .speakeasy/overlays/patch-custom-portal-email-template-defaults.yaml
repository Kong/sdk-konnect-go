overlay: 1.0.0
info:
  title: Omit defaults from the custom portal email template PATCH model
  version: 0.0.1
actions:
  - target: $.components.requestBodies.PatchCustomPortalEmailTemplatePayload.content['application/json'].schema.properties.enabled.default
    remove: true
